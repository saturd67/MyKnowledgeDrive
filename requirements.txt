# MyKnowledgeDrive - the packages the app imports directly.
#
# Everything else in .venv is pulled in by these (torch and numpy arrive with
# sentence-transformers, httplib2 with the Drive client), so it is not listed
# here - pin a transitive package only when a version of it actually breaks us.
#
#     pip install -r requirements.txt
#
# Tesseract itself is not a Python package: pytesseract only drives it, so the
# OCR steps need the Tesseract binary installed and on PATH as well.

# The desktop UI - flet-desktop is what opens the native window, and flet
# brings it in on Windows, but it is named here because a UI-less install of
# flet still imports fine and then fails at `ft.run`.
flet==0.86.5
flet-desktop==0.86.5

# The vector store, and the embedding model behind
# SentenceTransformerEmbeddingFunction - chroma does not require
# sentence-transformers itself, so it has to be asked for.
chromadb==1.3.7
sentence-transformers==5.2.0

# Google Drive: listing the folder and downloading the files.
google-api-python-client==2.187.0
google-auth==2.41.1

# Reading the downloaded files - .docx to HTML, HTML to text, and the images
# out of both.
mammoth==1.11.0
beautifulsoup4==4.14.3
pillow==12.0.0
pytesseract==0.3.13
sentence_transformers==5.2.0